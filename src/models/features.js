export default class Features {
  constructor(maxValence, maxEnergy, maxTempo, mode, genreGroup) {
    this.maxValence = maxValence;
    this.maxEnergy = maxEnergy;
    this.maxTempo = maxTempo;
    this.mode = mode;
    this.genreGroup = genreGroup;
  }
}